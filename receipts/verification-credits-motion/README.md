# Slower credits and vacuum gags

The roll now lasts 60 seconds instead of 48. Three staggered native SVG vacuums brake, peek and turn back, or reverse past the reader. Wheels rotate, eyes blink and brief dust puffs accompany the turns. Captions appear during each gag. A reserved lower strip keeps these effects below the credit text; landscape uses smaller robots and an adjusted introduction.

All motion uses the same clock, so Pause stops the text, vacuums, captions, wheels and dust. Reduced motion keeps a manually scrollable credit list without the passing robots. Skip, Made by Rapido and the Director discovery flow retain their existing actions. The new Inspirations block currently describes the game's influences without trademarked game names, pending the user's wording choice.

`browser.json` records real Settings clicks and read-only presentation observations. The probe checks the brake hold, subsequent departure, turn back toward the entry edge, backwards travel while facing forward, pause and resume, 60-second natural completion, phone controls, the Inspirations block, landscape, skip and reduced motion. A final placement adjustment removes the initial blank offset on short screens; `landscape.json` verifies that the opening heading fits completely inside its viewport after that change. `ship-check.txt` records the unmodified recipe's 39-module check. Source hashes cover the two changed runtime files.

The screenshots use local Chrome in desktop and phone layouts, not physical devices. This pass changes only credits presentation; no combat, leaderboard or multiplayer rules changed. Previous broader verification remains in `../verification-leaderboards/`. No new official gate, Atlas operation, deployment or submission is claimed here.
