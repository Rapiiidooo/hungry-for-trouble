# Mobile presentation

The owner reports oversized phone health/ammunition HUD and overlapping upgrade illustrations. Keep gameplay, touch targets, score replay and save formats unchanged.

1. Capture the current HUD and actual upgrade-card renderer at small and standard portrait sizes and phone landscape.
2. Compact status panels without hiding health, ammunition or shield state. Replace overlapping absolute-positioned upgrade art with an intrinsic responsive layout.
3. Inspect all eight illustrations, high-level values and desktop layout. Verify real touch movement/fire/dash and the official shipping check.
4. Publish and deploy the verified runtime, rerun the unchanged public phone/4G gate, and update the existing entry to the same commit and unedited verdict. Preserve the newly supplied wallet.
5. Save precise evidence, update the handoff and remove this completed tracker.

The wallet update is already published in entry PR #6. The existing stale commit/verdict bot thread was resolved after verifying that both name the deployed analytics commit. Browser plugin discovery returned no connected browser; use the established disposable Chrome/Puppeteer fallback for local visual checks.
